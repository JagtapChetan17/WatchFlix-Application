<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link rel="icon" href="./logo.png">
    <style>
        @keyframes gradient-bg {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(270deg, #e50914, #b31217, #8e0e15, #450008);
            background-size: 400% 400%;
            animation: gradient-bg 10s ease infinite;
        }

        .login-container {
            background-color: rgba(20, 20, 20, 0.9);
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
            padding: 2rem;
            width: 100%;
            max-width: 400px;
            text-align: center;
            position: relative;
        }

        h1 {
            font-size: 2rem;
            color: #fff;
            margin-bottom: 1.5rem;
        }

        p {
            color: #ff4c4c;
            margin-bottom: 1rem;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        input {
            margin-bottom: 1rem;
            padding: 0.8rem;
            font-size: 1rem;
            border: 1px solid #555;
            background-color: #222;
            color: #fff;
            border-radius: 5px;
            transition: border-color 0.3s, box-shadow 0.3s;
        }

        input:focus {
            outline: none;
            border-color: #e50914;
            box-shadow: 0 0 8px rgba(229, 9, 20, 0.5);
        }

        button {
            padding: 0.8rem;
            font-size: 1rem;
            color: #fff;
            background-color: #e50914;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.2s;
        }

        button:hover {
            background-color: #b31217;
            transform: scale(1.05);
        }

        .icon {
            width: 50px;
            height: 50px;
            margin-bottom: 1rem;
        }

        .icon-container {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-bottom: 1rem;
        }

        a {
            color: #e50914;
            text-decoration: none;
            font-size: 0.9rem;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="icon-container">
            <img src="https://cdn-icons-png.flaticon.com/512/456/456212.png" alt="Admin Icon" class="icon">
            <img src="https://cdn-icons-png.flaticon.com/512/3064/3064155.png" alt="Login Icon" class="icon">
            <img src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png" alt="User Icon" class="icon">
        </div>
        <h1>Admin Login</h1>
        {{#if errorMessage}}
        <p>{{errorMessage}}</p>
        {{/if}}
        <form action="/admin/login" method="POST">
            <input placeholder="Username" type="text" id="username" name="username" required>
            <input placeholder="Password" type="password" id="password" name="password" required>
            <button type="submit">Login</button>
        </form>
        <a href="/admin/register">Not an admin? Register an Admin.</a>
    </div>
</body>
</html>
