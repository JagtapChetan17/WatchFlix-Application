<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Register</title>
    <link rel="icon" href="./logo.png">
    <style>
        @keyframes gradient-bg {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }

        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(270deg, #1e3c72, #2a5298, #23395d, #141e30);
            background-size: 400% 400%;
            animation: gradient-bg 8s ease infinite;
        }

        .register-container {
            background-color: rgba(20, 30, 48, 0.95);
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
            padding: 2rem;
            width: 100%;
            max-width: 400px;
            text-align: center;
            position: relative;
        }

        h1 {
            font-size: 1.8rem;
            color: #e50914;
            margin-bottom: 1.5rem;
        }

        p {
            color: #e63946;
            margin-bottom: 1rem;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        input {
            margin-bottom: 1rem;
            padding: 0.8rem;
            font-size: 1rem;
            border: 1px solid #333;
            border-radius: 5px;
            background-color: #141414;
            color: #fff;
            transition: border-color 0.3s, box-shadow 0.3s;
        }

        input:focus {
            outline: none;
            border-color: #e50914;
            box-shadow: 0 0 8px rgba(229, 9, 20, 0.5);
        }

        button {
            padding: 0.8rem;
            font-size: 1rem;
            color: #ffffff;
            background-color: #e50914;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #b00610;
        }

        .icon {
            width: 50px;
            height: 50px;
            margin-bottom: 1rem;
        }

        .icon-container {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-bottom: 1rem;
        }

        a {
            display: block;
            margin-top: 1rem;
            color: #e50914;
            text-decoration: none;
            font-size: 0.9rem;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="register-container">
        <div class="icon-container">
            <img src="https://cdn-icons-png.flaticon.com/512/456/456212.png" alt="Admin Icon" class="icon">
        </div>
        <h1>Admin Register</h1>
        {{#if errorMessage}}
        <p>{{errorMessage}}</p>
        {{/if}}
        <form action="/admin/register" method="POST">
            <input placeholder="Username" type="text" id="username" name="username" required>
            <input placeholder="Password" type="password" id="password" name="password" required>
            <input placeholder="Secret Code" type="password" id="secretCode" name="secretCode" required>
            <button type="submit">Register</button>
        </form>
        <a href="/admin/login">Already Register as an Admin? Login here.</a>
    </div>
</body>
</html>
